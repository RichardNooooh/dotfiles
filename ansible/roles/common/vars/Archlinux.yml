---
# Arch Linux specific package list
# Arch uses 'python' instead of 'python3'
# fd package is named 'fd' not 'fd-find'

common_packages:
  - zsh
  - stow
  - curl
  - wget
  - git
  - unzip
  - zip
  - tar
  - make
  - gcc
  - python
  - python-pip
  - ripgrep
  - fd
  - tmux
