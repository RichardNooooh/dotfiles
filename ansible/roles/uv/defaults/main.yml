---
# UV role default variables

# Installation method: "mise" (default) or "standalone"
uv_install_method: "mise"

# URL for standalone installer
uv_installer_url: "https://astral.sh/uv/install.sh"

# Python packages to install via uv
tools:
  - debugpy
  - ruff
  - ty
