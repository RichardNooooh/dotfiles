---
- name: Bootstrap .dotfiles
  hosts: all
  become: true

  tasks: 
    - name: Install packages
      ansible.builtin.package:
        name:
          - zsh
          - stow
          - curl
          - wget 
          - zip
          - unzip
          - 
        state: present
